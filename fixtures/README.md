# Fixtures

Input repos, snapshots, and expected diffs. Fake data only.

## repositories

- `hono-pnpm-basic` — single-package Node + Hono + pnpm.
- `hono-npm-workspace` — npm workspaces with one Hono app and one library.
- `hono-ambiguous-apps` — two exported Hono apps, no default target.
- `tsconfig-js-only` — JavaScript tsconfig that must not be executed.
