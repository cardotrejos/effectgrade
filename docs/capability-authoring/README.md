# Capability authoring

Capability authoring is not open in this preview. Built-in capabilities only.

When the engine can compile, verify, and apply `core` and `hono-bridge`, this
directory will hold the step-by-step authoring guide from the product plan.
